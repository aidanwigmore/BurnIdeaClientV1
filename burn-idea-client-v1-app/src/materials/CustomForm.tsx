import React from 'react';

import FormControl from '@mui/material/FormControl';

function CustomForm() {
    return (
        <FormControl>
        </FormControl>
    );
}

export default CustomForm;  